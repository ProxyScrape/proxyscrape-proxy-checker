/**
 * Feature flags — flip these to toggle experimental features without touching
 * any of the production code paths.
 *
 * USE_TEXTAREA_INPUT: replaces the drop-zone + stats card in Input.jsx with
 * the unified textarea design in InputV2.jsx. Set to false to instantly revert.
 */
export const USE_TEXTAREA_INPUT = true;
