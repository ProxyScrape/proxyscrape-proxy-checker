name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  # Derived from the pushed tag (e.g. v2.0.0-canary or v2.0.0).
  # Used to inject appVersion into Go binaries and to set EP_PRE_RELEASE.
  APP_VERSION: ${{ github.ref_name }}
  EP_PRE_RELEASE: ${{ contains(github.ref_name, '-canary') }}

jobs:
  # ── Version consistency gate ──────────────────────────────────────────────────
  # Fails fast if package.json, Go appVersion, or the git tag disagree.
  # All build jobs depend on this so nothing is built from a mismatched state.
  check-versions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Check version consistency
        run: bash scripts/check-versions.sh "${{ github.ref_name }}"

  # ── JS bundle: built once, shared across all platform release jobs ────────────
  build-renderer:
    needs: [check-versions]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build renderer bundle
        run: npx electron-vite build --outDir dist
        env:
          POSTHOG_KEY: ${{ secrets.POSTHOG_KEY }}
          POSTHOG_API_HOST: ${{ secrets.POSTHOG_API_HOST }}
          POSTHOG_UI_HOST: ${{ secrets.POSTHOG_UI_HOST }}
          INTERCOM_APP_ID: ${{ secrets.INTERCOM_APP_ID }}

      - uses: actions/upload-artifact@v6
        with:
          name: renderer-dist
          path: dist/

  # ── Go binaries: Windows (cross-compiled from Linux with zig cc) ─────────────
  build-go-windows:
    needs: [check-versions]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: backend/go.mod
          cache: true
          cache-dependency-path: backend/go.sum

      - name: Install Zig
        run: |
          ZIG_VERSION="0.15.2"
          curl -fsSL "https://ziglang.org/download/${ZIG_VERSION}/zig-x86_64-linux-${ZIG_VERSION}.tar.xz" \
            | tar -xJ -C "$RUNNER_TEMP"
          echo "$RUNNER_TEMP/zig-x86_64-linux-${ZIG_VERSION}" >> "$GITHUB_PATH"

      - name: Build win-x64
        run: |
          GOOS=windows GOARCH=amd64 CGO_ENABLED=1 \
            CC="zig cc -target x86_64-windows-gnu" \
            go build -C backend -buildvcs=false \
            -ldflags="-s -w -X 'github.com/proxyscrape/checker-backend/internal/api.appVersion=${APP_VERSION#v}'" \
            -o ../bin/checker-win-x64.exe ./cmd/checker

      - name: Build win-arm64
        run: |
          GOOS=windows GOARCH=arm64 CGO_ENABLED=1 \
            CC="zig cc -target aarch64-windows-gnu" \
            go build -C backend -buildvcs=false \
            -ldflags="-s -w -X 'github.com/proxyscrape/checker-backend/internal/api.appVersion=${APP_VERSION#v}'" \
            -o ../bin/checker-win-arm64.exe ./cmd/checker

      - uses: actions/upload-artifact@v6
        with:
          name: go-win-bins
          path: bin/

  # ── Go binaries: Linux (x64 native, arm64 via GCC cross-compiler) ───────────
  build-go-linux:
    needs: [check-versions]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: backend/go.mod
          cache: true
          cache-dependency-path: backend/go.sum

      - name: Install libpcap headers + arm64 cross-compiler
        run: |
          sudo dpkg --add-architecture arm64

          # Ubuntu 24.04 uses deb822 format (/etc/apt/sources.list.d/ubuntu.sources).
          # Without Architectures: fields, apt tries to fetch arm64 from
          # security.ubuntu.com and azure mirrors, which 404 for non-amd64.
          # Replace the file with one that explicitly restricts amd64 sources
          # and adds ports.ubuntu.com for arm64.
          sudo tee /etc/apt/sources.list.d/ubuntu.sources > /dev/null << 'EOF'
          Types: deb
          URIs: http://azure.archive.ubuntu.com/ubuntu/
          Suites: noble noble-updates noble-backports
          Components: main restricted universe multiverse
          Architectures: amd64
          Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg

          Types: deb
          URIs: http://security.ubuntu.com/ubuntu/
          Suites: noble-security
          Components: main restricted universe multiverse
          Architectures: amd64
          Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg

          Types: deb
          URIs: http://ports.ubuntu.com/ubuntu-ports/
          Suites: noble noble-updates noble-backports noble-security
          Components: main restricted universe multiverse
          Architectures: arm64
          Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
          EOF

          sudo apt-get update -qq
          # gcc-aarch64-linux-gnu: GCC cross-compiler with its own sysroot/headers,
          # avoids the glibc version mismatch that occurs with zig cc + system headers.
          sudo apt-get install -y libpcap-dev libpcap-dev:arm64 gcc-aarch64-linux-gnu

      - name: Build linux-x64
        run: |
          CGO_ENABLED=1 GOOS=linux GOARCH=amd64 \
            go build -C backend -buildvcs=false \
            -ldflags="-s -w -X 'github.com/proxyscrape/checker-backend/internal/api.appVersion=${APP_VERSION#v}'" \
            -o ../bin/checker-linux-x64 ./cmd/checker

      - name: Build linux-arm64 (aarch64-linux-gnu-gcc cross-compile)
        run: |
          CGO_ENABLED=1 GOOS=linux GOARCH=arm64 \
            CC=aarch64-linux-gnu-gcc \
            CGO_LDFLAGS="-L/usr/lib/aarch64-linux-gnu" \
            go build -C backend -buildvcs=false \
            -ldflags="-s -w -X 'github.com/proxyscrape/checker-backend/internal/api.appVersion=${APP_VERSION#v}'" \
            -o ../bin/checker-linux-arm64 ./cmd/checker

      - uses: actions/upload-artifact@v6
        with:
          name: go-linux-bins
          path: bin/

  # ── Go binaries: macOS ───────────────────────────────────────────────────────
  build-go-macos:
    needs: [check-versions]
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: backend/go.mod
          cache: true
          cache-dependency-path: backend/go.sum

      - name: Build darwin-arm64
        run: |
          CGO_ENABLED=1 GOOS=darwin GOARCH=arm64 \
            go build -C backend -buildvcs=false \
            -ldflags="-s -w -X 'github.com/proxyscrape/checker-backend/internal/api.appVersion=${APP_VERSION#v}'" \
            -o ../bin/checker-darwin-arm64 ./cmd/checker

      - name: Build darwin-x64
        run: |
          CGO_ENABLED=1 GOOS=darwin GOARCH=amd64 \
            go build -C backend -buildvcs=false \
            -ldflags="-s -w -X 'github.com/proxyscrape/checker-backend/internal/api.appVersion=${APP_VERSION#v}'" \
            -o ../bin/checker-darwin-x64 ./cmd/checker

      - uses: actions/upload-artifact@v6
        with:
          name: go-macos-bins
          path: bin/

  # ── Electron: Windows installer + portable ──────────────────────────────────
  release-windows:
    runs-on: windows-latest
    needs: [build-renderer, build-go-windows]
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Cache Electron binary
        uses: actions/cache@v5
        with:
          path: ${{ env.LOCALAPPDATA }}\electron\Cache
          key: electron-win-${{ hashFiles('package.json') }}

      - uses: actions/download-artifact@v7
        with:
          name: renderer-dist
          path: dist

      - uses: actions/download-artifact@v7
        with:
          name: go-win-bins
          path: bin

      - name: Install dependencies
        run: npm ci

      - name: Package & publish Windows
        run: npx electron-builder --config electron-builder.config.js --win --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          POSTHOG_KEY: ${{ secrets.POSTHOG_KEY }}
          POSTHOG_API_HOST: ${{ secrets.POSTHOG_API_HOST }}
          POSTHOG_UI_HOST: ${{ secrets.POSTHOG_UI_HOST }}
          INTERCOM_APP_ID: ${{ secrets.INTERCOM_APP_ID }}

  # ── Electron: Linux AppImage ─────────────────────────────────────────────────
  release-linux:
    runs-on: ubuntu-latest
    needs: [build-renderer, build-go-linux]
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Cache Electron binary
        uses: actions/cache@v5
        with:
          path: ~/.cache/electron
          key: electron-linux-${{ hashFiles('package.json') }}

      - uses: actions/download-artifact@v7
        with:
          name: renderer-dist
          path: dist

      - uses: actions/download-artifact@v7
        with:
          name: go-linux-bins
          path: bin

      - name: Make Linux binaries executable
        run: chmod +x bin/checker-linux-*

      - name: Install dependencies
        run: npm ci

      - name: Package & publish Linux
        run: npx electron-builder --config electron-builder.config.js --linux --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          POSTHOG_KEY: ${{ secrets.POSTHOG_KEY }}
          POSTHOG_API_HOST: ${{ secrets.POSTHOG_API_HOST }}
          POSTHOG_UI_HOST: ${{ secrets.POSTHOG_UI_HOST }}
          INTERCOM_APP_ID: ${{ secrets.INTERCOM_APP_ID }}

  # ── Electron: macOS DMG + zip ────────────────────────────────────────────────
  release-macos:
    runs-on: macos-latest
    needs: [build-renderer, build-go-macos]
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Cache Electron binary
        uses: actions/cache@v5
        with:
          path: ~/Library/Caches/electron
          key: electron-macos-${{ hashFiles('package.json') }}

      - uses: actions/download-artifact@v7
        with:
          name: renderer-dist
          path: dist

      - uses: actions/download-artifact@v7
        with:
          name: go-macos-bins
          path: bin

      - name: Make macOS binaries executable
        run: chmod +x bin/checker-darwin-*

      - name: Install dependencies
        run: npm ci

      - name: Package & publish macOS (unsigned)
        run: npx electron-builder --config electron-builder.config.js --mac --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          POSTHOG_KEY: ${{ secrets.POSTHOG_KEY }}
          POSTHOG_API_HOST: ${{ secrets.POSTHOG_API_HOST }}
          POSTHOG_UI_HOST: ${{ secrets.POSTHOG_UI_HOST }}
          INTERCOM_APP_ID: ${{ secrets.INTERCOM_APP_ID }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
